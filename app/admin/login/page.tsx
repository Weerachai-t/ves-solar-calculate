'use client';
import {FormEvent,useState} from 'react';
import {useRouter} from 'next/navigation';
export default function AdminLogin(){const router=useRouter();const [password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');const r=await fetch('/api/admin/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({password})});if(r.ok){router.push('/admin');router.refresh()}else{const d=await r.json().catch(()=>({}));setError(d.error||'เข้าสู่ระบบไม่สำเร็จ')}setBusy(false)}return <main className="adminLogin"><form onSubmit={submit} className="loginCard"><div className="brand"><span className="ves">VES</span><span className="dot"/><b>Solar Admin</b></div><p className="eyebrow">PHASE 3.2 · SECURE CRM</p><h1>เข้าสู่ระบบผู้ดูแล</h1><p>จัดการ Lead, Project และขั้นตอนเสนอราคาของ VES Solar Calculate</p><label>Admin Password<input autoFocus type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="กรอกรหัสผ่าน"/></label>{error&&<div className="loginError">{error}</div>}<button disabled={busy}>{busy?'กำลังตรวจสอบ...':'เข้าสู่ Admin Dashboard'}</button><a href="/">← กลับหน้า Solar Calculate</a></form></main>}
